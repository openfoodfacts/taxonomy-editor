{
  "devDependencies": {
    "prettier": "3.0.3",
    "prettier-plugin-toml": "^1.0.0"
  },
  "scripts": {
    "lint": "prettier --plugin=prettier-plugin-toml --write \"**/*.{json,md,yml,yaml,toml}\"",
    "lint:check": "prettier --plugin=prettier-plugin-toml --check \"**/*.{json,md,yml,yaml,toml}\""
  }
}
